
const Board = {
	action(phrase, stdOut, callback) {
		
	},
	dispatch(event) {
		let Self = Transmit,
			el;
		// console.log(event);
		switch (event.type) {
			case "select-color":
				console.log(event);
				break;
		}
	}
};
